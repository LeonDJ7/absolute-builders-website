{
  "name": "absolute-builders-server",
  "version": "0.0.0",
  "scripts": {
    "server": "concurrently \"nodemon server.js\" \"cd client && ng serve\""
  },
  "dependencies": {
    "concurrently": "^6.2.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  }
}
